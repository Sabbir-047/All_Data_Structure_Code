#include <bits/stdc++.h>
using namespace std;

int main()
{
    /// watch video
    // Basically -> Time complexity is a method where we can solve a problem in a more efficient way. So that we don't get any Time limit exceeded error. Which is (TLE)
    return 0;
}