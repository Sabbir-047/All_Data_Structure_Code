#include <bits/stdc++.h>
using namespace std;

int main()
{
    /*
    In this code the loop will run until it's reaches the n value.
    If n = 10; then it will run 10 times. 
    If n = 19999; then it will run 19999 times.


    So, If a code is dependent on the number of input or n. Then the time complexity for this code is. O(n). 
     
    O(n) -> order of n.
    
    */


    int n;
    cin >> n;
    for (int i = 0; i < n; i++){
        cout << "fweewf";
    }
    return 0;
}