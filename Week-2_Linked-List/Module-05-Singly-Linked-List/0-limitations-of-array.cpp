#include <bits/stdc++.h>
using namespace std;

int main()
{
    /*
            < --- Theory --- > 
        Array and vectors take values sequentially. With their data-type preferences.

        What's wrong with that ? 
        -> We are not going to able to insert a data sequentially if we insert a data middle. Where the first input data is bigger than this. 


        Note ---> Watch the picture or module.
    */
    return 0;
}